#include <stdio.h>

/**
 * main - prints all single numbers in base 10 starting from 0
 *
 * Return: always turns 0(Excellent)
 */

int main(void)
{
	int k;

	for (k = 0; k <= 9; k++)
	{
	printf("%d", k);
	}
	printf("\n");
	return (0);
}


#include "main.h"

/**
 * main - print in lowercase letter
 *
 * Return: always turns 0 on the screen
 */


void print_alphabet(void);

{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}

#include "lists.h"
#include <stdio.h>

/**
 * print_listint_safe - prints the number of nodes in a safe loop in a linked list
 * @head: pointer to the linked list
 *
 * Return: the number of nodes in the list, if the functions fails, 
 * exit the program with status 98
 */

